<?php

declare(strict_types=1);

use Infocyph\Console\Scheduling\Schedule;

return static function (Schedule $schedule): void {
    // Define scheduled commands here. Example:
    // $schedule->command('reports:daily')->dailyAt('02:00')->onOneServer()->withoutOverlap();
};
