<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Signed URLs
    |--------------------------------------------------------------------------
    |
    | Enable verification support for routes using the "signed" middleware.
    | Signing keys, expiry, and URL options are configured in router.php.
    | Accepted values: `true|false`.
    |
    */
    'signed_urls' => true,
];
